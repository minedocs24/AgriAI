/**
 * useAuth Hook
 * 
 * Re-export dell'hook useAuth dal AuthContext per compatibilità
 * e per rendere più semplice l'import
 */

export { useAuth, useAuthGuard } from '@/contexts/AuthContext';